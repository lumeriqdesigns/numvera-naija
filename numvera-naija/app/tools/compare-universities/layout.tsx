import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Compare University JAMB Aggregates | Numvera Naija",
  description: "Compare two Nigerian universities' aggregate models with the same UTME and Post-UTME scores.",
};
export default function L({ children }: { children: React.ReactNode }) {
  return children;
}
