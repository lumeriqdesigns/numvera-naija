import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "NECO & NABTEB Points Calculator | Numvera Naija",
  description: "Calculate NECO or NABTEB best 5 and best 8 grade points for Nigerian admission planning.",
};
export default function L({ children }: { children: React.ReactNode }) {
  return children;
}
