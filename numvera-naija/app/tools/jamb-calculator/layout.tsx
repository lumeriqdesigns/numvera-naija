import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "JAMB Aggregate & Cut-off Calculator Nigeria 2026 | Numvera Naija",
  description:
    "Free JAMB aggregate calculator with UTME cut-offs for UNILAG, UI, OAU, ABU and 100+ universities. 50:50, 40:60 and O'Level models.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
